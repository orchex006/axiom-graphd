# NEGATIVE FIXTURE - must be rejected

Live ignored data, the staged export and merged-source regeneration are named
here, but the worked example has no end sentinel, so the driver must reject it.

<!-- BEGIN GIT CHECKPOINT EXAMPLE -->
```json
{ "union_merge_generated_graphs": false }
```
