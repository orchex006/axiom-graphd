// Component-local boundary fixture for
// graph_analyze::typescript::declarations (task F-005). A truncated class must
// be reported as a diagnostic with partial coverage, never silently accepted as
// an empty complete analysis. The machine-readable half is
// declarations_unbalanced.expected.json.

export class Broken {
