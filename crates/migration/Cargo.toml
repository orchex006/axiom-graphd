[package]
name = "axiom-migration"
description = "Axiom graph engine: read-only legacy layout discovery and deterministic, ownership-aware V1 to V2 migration plans"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
graph-core = { version = "0.1.0", path = "../graph-core" }
axiom-platform = { version = "0.1.0", path = "../platform" }
sha2.workspace = true

[dev-dependencies]
tempfile.workspace = true
