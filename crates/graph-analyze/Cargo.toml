[package]
name = "graph-analyze"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Static-analysis adapters, pinned grammar registry and declaration extraction for axiom-graphd."

[dependencies]
graph-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# The L3 HTTP join resolves a host only through the registered solution document
# (task H-004), so its tests read the alias map from `axiom-config` instead of
# writing a host table by hand.
axiom-config = { version = "0.1.0", path = "../config" }

[lints]
workspace = true